fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# OCR stack
rapidocr_onnxruntime>=1.2.3
easyocr>=1.7.1
paddlepaddle>=2.6.2
paddleocr>=2.7.3
pdf2image>=1.17.0
pypdfium2>=5.0.0
Pillow>=10.3.0
numpy>=1.26.4
opencv-python-headless>=4.10.0

# NLP entity-extraction layer (English text only).
# en_core_web_sm is listed as a direct URL so 'pip install -r requirements.txt'
# installs both the spaCy library AND the model in one step (local dev / CI).
# In Docker the model is downloaded via 'python -m spacy download en_core_web_sm'
# (see Dockerfile) to keep it in its own cached layer.
spacy>=3.7.5
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# Testing
pytest>=8.2.2
pytest-cov>=5.0.0
httpx>=0.27.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4

# OCR stack
paddlepaddle==2.6.2
paddleocr==2.7.3
pdf2image==1.17.0
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.10.0.84

# Optional NLP entity-extraction layer (English text only).
# The 'en_core_web_sm' model itself is downloaded separately — see Dockerfile
# (`python -m spacy download en_core_web_sm`) — it is not a pip package.
spacy==3.7.5

# Testing (also used by CI pipeline)
pytest==8.2.2
pytest-cov==5.0.0
httpx==0.27.0